# Fine-tuning dependencies (install separately – requires GPU + ~12GB+ VRAM)
# pip install -r requirements-training.txt

torch>=2.2.0
transformers>=4.40.0
peft>=0.10.0
trl>=0.8.0
datasets>=2.18.0
bitsandbytes>=0.43.0
accelerate>=0.29.0
scipy>=1.12.0
